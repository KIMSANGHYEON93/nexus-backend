interface:
  display_name: "Nexus Backend"
  short_description: "Repo-specific patterns and workflows for nexus-backend"
  default_prompt: "Use the nexus-backend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true